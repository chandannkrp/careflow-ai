# Base URL of the deployed backend, e.g. https://careflow-backend.onrender.com
# No trailing slash — API paths already start with /api.
# Leave empty in local dev: Vite proxies /api to http://127.0.0.1:8080.
VITE_API_BASE_URL=
